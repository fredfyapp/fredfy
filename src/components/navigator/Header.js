import React from 'react';
import PropTypes from 'prop-types';

const Header = ({}) => {
  return (
    <header className='header'>
      <h2>Header</h2>
    </header>
  );
}

Header.propTypes = {
  // : PropTypes.
};

export default Header;
