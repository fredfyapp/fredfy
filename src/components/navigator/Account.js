// ********** REACT ********** //
import React from 'react';
import PropTypes from 'prop-types';

const Account = ({}) => {
  return (
    <div>
      <h2>Account</h2>
    </div>
  );
}

Account.propTypes = {
  // : PropTypes.
};

export default Account;
