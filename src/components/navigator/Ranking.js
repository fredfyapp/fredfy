import React from 'react';
import PropTypes from 'prop-types';

const Ranking = ({}) => {
  return (
    <div>
      Ranking
    </div>
  );
}

Ranking.propTypes = {
  // : PropTypes.
};

export default Ranking;
