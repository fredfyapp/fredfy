// ********** REACT ********** //
import React from 'react';
import PropTypes from 'prop-types';

const Footer = ({}) => {
  return (
    <footer className='footer'>
      <h2>Footer</h2>
    </footer>
  );
}

Footer.propTypes = {
  // : PropTypes.
};

export default Footer;
