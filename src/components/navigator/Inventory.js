import React from 'react';
import PropTypes from 'prop-types';

const Inventory = ({}) => {
  return (
    <div>
      <h2>Inventory</h2>
    </div>
  );
}

Inventory.propTypes = {
  // : PropTypes.
};

export default Inventory;
