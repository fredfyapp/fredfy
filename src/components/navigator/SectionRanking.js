import React from 'react';
import PropTypes from 'prop-types';

const SectionRanking = ({}) => {
  return (
    <div>
      <h3>SectionRanking</h3>
    </div>
  );
}

SectionRanking.propTypes = {
  // : PropTypes.
};

export default SectionRanking;
