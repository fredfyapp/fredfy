// ********** REACT ********** //
import React from 'react';
import PropTypes from 'prop-types';

const WorldCard = ({}) => {
  return (
    <div>
      <h2>WorldCard</h2>
    </div>
  );
}

WorldCard.propTypes = {
  // : PropTypes.
};

export default WorldCard;
